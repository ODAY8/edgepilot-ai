fastapi>=0.115
uvicorn[standard]>=0.32
python-dotenv>=1.0
pydantic>=2.9
python-multipart>=0.0.12
groq>=0.11
google-genai>=1.0
# Already an implicit dependency of google-genai; listed explicitly
# because services/vision.py now imports httpx.TransportError directly
# to detect retryable connection-level failures.
httpx>=0.27
opencv-python-headless>=4.10
pytest>=8.0
Pillow>=10.0
psycopg[binary]>=3.1
# psycopg's core package has no built-in connection pool -- pooling is a
# separate, official companion package from the same project. Required
# to satisfy "reuse a pool, never open a new connection per request"
# for the PostgreSQL/Supabase backend in app/database/database.py.
psycopg_pool>=3.1
